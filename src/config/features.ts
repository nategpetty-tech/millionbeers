export const friendsFeatureEnabled = false;
// Keep automatic venue matching available for a future re-enable while retaining city stamps.
export const checkInVenueEnabled = false;
export const screenshotDemoEnabled = process.env.EXPO_PUBLIC_SCREENSHOT_DEMO === "true";
