export const friendsFeatureEnabled = false;
// Keep the location work available for a future re-enable without prompting users today.
export const checkInLocationEnabled = false;
export const screenshotDemoEnabled = process.env.EXPO_PUBLIC_SCREENSHOT_DEMO === "true";
